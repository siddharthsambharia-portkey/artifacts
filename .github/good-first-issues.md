# Good first issues

1. Add chromedp e2e test for guestbook example
2. Add `artifact logs --follow` tail mode
3. Document Entra ID OIDC setup step-by-step
4. Add MinIO on-prem self-hosting guide
5. Improve admin console site search/filter
6. Add `artifact deploy --dry-run` showing file diff
7. Add Homebrew tap formula in separate repo
8. Add cosign signing to release workflow
9. Add SQLite→Postgres migration helper script
10. Add rate limit headers (`X-RateLimit-Remaining`) to API responses
