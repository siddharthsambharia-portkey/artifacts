# Live Poll

Realtime voting with `artifact.db` + `subscribe()`.

```bash
artifact deploy examples/live-poll
open http://live-poll.localhost:8443
```

Open two browser tabs — votes update live without refresh.
