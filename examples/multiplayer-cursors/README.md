# Multiplayer Cursors

WebSocket rooms + presence via `artifact.ws`.

```bash
artifact deploy examples/multiplayer-cursors
open http://multiplayer-cursors.localhost:8443
```

Open in two windows side-by-side and move your mouse.
