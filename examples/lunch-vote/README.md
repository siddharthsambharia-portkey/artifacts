# Lunch Vote

Database + websockets + Slack notifications.

```bash
artifact deploy examples/lunch-vote
open http://lunch-vote.localhost:8443
```

Configure `notify.slack` in artifact.yaml for Slack pings.
